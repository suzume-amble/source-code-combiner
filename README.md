# Code Combiner

VSCode上で指定したディレクトリ内のソースコードを1つのMarkdownファイルへ結合する拡張機能です。

## 主な機能

- 指定したディレクトリ内のソースコードを再帰的に収集
- ディレクトリツリーを先頭に付加したMarkdownを生成
- `.gitignore` を考慮して不要なファイルを除外
- ソースコードを言語別のMarkdownコードブロックで出力
- 出力サイズ警告や上書き確認に対応
- `settings.json` による対象ファイル・出力先などのカスタマイズ
